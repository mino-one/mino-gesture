---
description: 全局编码规范：组件化、封装、文件长度控制
alwaysApply: true
---

# Core Coding Standards

- 前端页面与复杂视图必须组件化，避免单文件堆叠业务逻辑。
- 出现 2 处以上复用的逻辑必须抽离（`hooks`、`utils`、`services`），禁止复制粘贴。
- 类似工具函数统一放到同一模块并暴露清晰 API，命名语义化，避免临时函数散落。
- 单文件建议不超过 300 行；接近 260 行时必须拆分子组件或提取函数。
- 优先“高内聚、低耦合”：UI、状态、数据处理、平台调用分层清晰。
- 变更时尽量保持小步提交，避免一次提交混合重构和新功能。
