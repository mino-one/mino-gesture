---
description: Tauri/Rust 规范：模块边界、命令设计、错误处理与文件规模控制
globs: src-tauri/**/*.{rs,toml,json}
alwaysApply: false
---

# Tauri Rust Conventions

- Rust 核心按职责拆分：`input`、`gesture`、`rules`、`actions`、`config`、`tray`，禁止把业务堆在 `lib.rs`。
- `#[tauri::command]` 只做参数校验和编排，不写复杂逻辑；复杂逻辑下沉到对应模块函数。
- 命令入参与出参保持稳定、可序列化，命名统一 `camelCase`；避免前后端字段风格不一致。
- 错误处理统一：模块内部优先 `anyhow/thiserror`，命令边界统一转换为可读字符串返回前端。
- 配置读写必须集中在 `config` 模块，禁止在其他模块直接读写配置文件。
- 新增动作类型必须通过 `actions` 统一入口执行，避免在命令里直接调用系统命令。
- 任何高频逻辑（输入采样、识别、匹配）保持在 Rust 侧；前端只负责展示和配置。
- Rust 单文件不超过 300 行；接近阈值时拆分子模块（如 `rules/matcher.rs`、`rules/model.rs`）。
- `tauri.conf.json` 与 capability 文件改动应最小化，修改前确认权限与目标窗口范围，避免过度放权。
- 日志使用 `tracing`，日志内容以调试与诊断为目标，避免输出敏感信息。
